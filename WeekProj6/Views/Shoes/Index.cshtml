@model IEnumerable<WeekProj6.Models.Sho>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

<div class="row">
    @foreach (var item in Model)
    {
        <div class="col-sm-6 col-md-6">
            <div class="caption text-center">

                <h3>@Html.DisplayFor(modelItem => item.Name)</h3>

                @if (item.Photo != "...")
                {
                    <a href="@Url.Action("Details", "Shoes", new { id = item.ShoeID })" class="thumbnail">
                        <img src=@Url.Content(item.Photo)>

                    </a>
                }
                    <p>@Html.DisplayFor(modelItem => item.Type)</p>
                    <p>@Html.DisplayFor(modelItem => item.Color)</p>
                    <p>@Html.DisplayFor(modelItem => item.Season)</p>
                    <p>@Html.DisplayFor(modelItem => item.Occasion)</p>

            </div>
        </div>

        <div>
            @Html.ActionLink("Edit", "Edit", new { id = item.ShoeID }) |
            @Html.ActionLink("Details", "Details", new { id = item.ShoeID }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.ShoeID })
        </div>

    }


</div>